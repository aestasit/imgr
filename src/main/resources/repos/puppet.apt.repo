# Puppetlabs products
deb ${basePuppetRepoUrl} precise main
deb-src ${basePuppetRepoUrl} precise main

# Puppetlabs dependencies
deb ${basePuppetRepoUrl} precise dependencies
deb-src ${basePuppetRepoUrl} precise dependencies
